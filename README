* Requirements
  - wget
  - subversion

* Install

1. git clone  git://github.com/natsag2000/dot-emacs.git
2. git checkout --track -b tsgan origin/tsgan
2. cd dot-emacs/elisp
   ## following step will copy "elisp" folder under home directory and copies ~/.emacs file also
   ## Дараах алхам нь шаардлагатай бүх файл болон ~/.emacs файлыг гэрийн санд байгаа elisp сан уруу хуулна.
3. make install
   ## хуулсны дараа ~/elisp сан уруу очиж суулгах үйлдлийг гүйцэтгэнэ.
4. cd ~/elisp
   # next step will download all necessary elisp files
5. make all

start Emacs and enjoy :)
