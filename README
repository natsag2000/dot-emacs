* Requirements
  - wget
  - subversion

* Install

1. git clone  git://github.com/natsag2000/dot-emacs.git
2. cd dot-emacs
3. git checkout --track -b tsgan origin/tsgan
4. make all ( on BSD machine gmake all )

start Emacs and enjoy :)
